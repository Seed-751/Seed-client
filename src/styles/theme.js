const color = {
  blue: "#83CDDD",
  white: "#fff",
  grey: "#F0F2FA",
};

const padding = {
  small: "15px",
  default: "20px",
  big: "40px",
};

const theme = {
  color,
  padding,
};

export default theme;
